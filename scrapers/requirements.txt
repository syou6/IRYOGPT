# スクレイパーサービス依存パッケージ
# pip install -r scrapers/requirements.txt

# ブラウザ自動化（bot検知回避、asyncネイティブ）
zendriver>=0.15.0

# HTTP クライアント（Supabase API通信）
httpx>=0.27.0

# APIサーバー（よやくらくからのリアルタイムチェック用）
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0

# 環境変数
python-dotenv>=1.0.0
